function SearchForm() {
  return (
    <form>
      <input
        id="name"
        placeholder="Element name"
        type="text"
        // value & onChange (event) function
      />
    </form>
  );
}

export default SearchForm;
